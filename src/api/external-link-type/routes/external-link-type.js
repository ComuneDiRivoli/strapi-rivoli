'use strict';

/**
 * external-link-type router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::external-link-type.external-link-type');
