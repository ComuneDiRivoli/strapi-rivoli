'use strict';

/**
 * external-link-type service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::external-link-type.external-link-type');
